import { RepoOverlayDirective } from './repo-overlay.directive';

describe('RepoOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new RepoOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
